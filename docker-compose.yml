version: '3'
services:
  web:
    image: nginx:alpine
    container_name: pdfedit
    restart: unless-stopped
    volumes:
      - .:/usr/share/nginx/html:ro
    networks:
      - npm_network

networks:
  npm_network:
    external: true
